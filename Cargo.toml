[workspace]
resolver = "2"
members = [
    "mlsh-cli",
    "mlsh-signal",
    "mlsh-crypto",
    "mlsh-protocol",
]

[profile.release]
opt-level = "z"
lto = true
strip = true
codegen-units = 1
panic = "abort"
