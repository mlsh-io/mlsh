+++
title = "CLI Reference"
description = "Commands, flags, and configuration for the mlsh CLI."
weight = 2
sort_by = "weight"
+++

The `mlsh` CLI drives the node lifecycle: setup, invite, adopt, connect. It also controls the tunnel daemon (`mlshtund`) and inspects cluster state.
